# Set to true to use real HTTP API services instead of in-memory mock services
VITE_USE_HTTP_SERVICES=true

# Base URL for your backend API (required when VITE_USE_HTTP_SERVICES=true)
VITE_API_BASE_URL=http://localhost:3000/api
